---
description: Install mcp-md-index server
---
1. Run `go install .` in the project root to install the binary to your GOPATH.
2. Alternatively, build and run locally: `go build -o mcp-md-index . && ./mcp-md-index`
